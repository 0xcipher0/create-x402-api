# create-x402-api
A CLI tool to quickly scaffold x402 API projects.

## Installation

Run the command and follow the prompts.

```shell
npx create-x402-api
```

## Usage

```shell
cd your_project-name
npm run dev
```
